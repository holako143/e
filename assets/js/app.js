import { loadSettings, loadEmojis, loadHistory } from './modules/state.js';import { renderEmojis, renderHistory, updateCharCount, applySettingsToUI, switchTab } from './modules/ui.js';import { setupEventListeners } from './modules/events.js';function initializeApp() {    console.log("Initializing Emoji Cipher Pro v2 (Modular)...");        loadSettings();    loadEmojis();    loadHistory();        applySettingsToUI();    renderEmojis();    renderHistory();    updateCharCount();            setupEventListeners();        switchTab('cipher');    console.log("Application initialized successfully.");        }if (document.readyState === 'loading') {    document.addEventListener('DOMContentLoaded', initializeApp);} else {    initializeApp();}
